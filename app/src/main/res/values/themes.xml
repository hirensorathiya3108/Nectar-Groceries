<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NectarGroceries" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="android:statusBarColor">@color/themeColor</item>
    </style>

    <style name="Theme.NectarGroceries" parent="Base.Theme.NectarGroceries" />

    <style name="ProgressBarSecondryTheme" parent="@style/ThemeOverlay.AppCompat.Light">
        <item name="colorAccent">@color/themeColor</item>
        <!--        old-->
        <!--        <item name="colorAccent">@color/almost_black</item>-->
    </style>

    /for BottomNavigationView
    <style name="BottomNavigationMenuTextStyle">
        <item name="android:fontFamily">@font/pangram_regular</item>
    </style>

    //for BottomSheetDialog

    <style name="SheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:colorBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.2</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
    </style>

</resources>